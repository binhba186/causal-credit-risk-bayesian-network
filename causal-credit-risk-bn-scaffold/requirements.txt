# ==========================================================
# CausalCredit-BN — requirements.txt
# Trích xuất trực tiếp từ import/pip install trong notebook gốc
# (thesis_Autralian_data.ipynb — Australian / German / LendingClub)
# ==========================================================

# --- Core data & numerical computing ---
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0

# --- Causal / Bayesian Network modeling ---
pgmpy>=0.1.24,<0.2.0
networkx>=3.1,<4.0

# --- Machine Learning (baselines & feature selection) ---
scikit-learn>=1.3,<1.5
xgboost>=2.0,<3.0
imbalanced-learn>=0.11,<0.13    # SMOTENC

# --- Explainability (đối chiếu Causal BN vs correlation-based XAI) ---
shap>=0.44,<0.46
lime>=0.2.0.1

# --- Visualization ---
matplotlib>=3.7,<4.0
graphviz>=0.20,<0.21            # vẽ cấu trúc DAG (yêu cầu cài graphviz binary ở hệ điều hành)

# --- Jupyter / interactive plotting (tuỳ chọn, dùng trong notebook gốc) ---
jupyter>=1.0
ipympl>=0.9,<0.10               # %matplotlib widget cho biểu đồ tương tác

# ==========================================================
# Cài đặt:
#   pip install -r requirements.txt
#
# Lưu ý:
# - `graphviz` (python package) cần binary Graphviz cài sẵn ở OS:
#     Ubuntu/Debian : sudo apt-get install graphviz
#     macOS         : brew install graphviz
#     Windows       : https://graphviz.org/download/
# - `ipympl` chỉ cần thiết nếu chạy notebook với %matplotlib widget
#   (không bắt buộc khi chạy pipeline qua scripts/run_pipeline.py)
# ==========================================================
